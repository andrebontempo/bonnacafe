services:
  bonnacafe:
    build: .
    container_name: bonnacafe
    restart: always
    environment:
      - PORT=80
      - NODE_ENV=production
      - ADMIN_PASSWORD=bonna123
    volumes:
      - ./data:/app/data
    networks:
      - proxy

networks:
  proxy:
    external: true
