services:
  bonnacafe:
    image: nginx:latest
    container_name: bonnacafe
    restart: always
    volumes:
      - ./bonnacafe:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - proxy

networks:
  proxy:
    external: true
